## 모델

```javascript
const userModel = {
  users: [],
  fetchUsers: async () => {
    const response = await fetch("/api/users");
    userModel.users = await response.json();
  },
};
```

- 이상적인 아키텍처에서는 뷰는 모델의 존재를, 모델은 뷰의 존재를 직접 알아서는 안됩니다.
- 이 때 중재자가 필요합니다

  - MVC 패턴에서는 컨트롤러
  - MVP 패턴에서는 프레젠터
  - MVVM 패턴에서는 뷰모델
  - Flux 아키텍처에서는 액션과 스토어의 조합을 통한 단방향 파이프라인

- 컴포넌트 내부의 중재자 로직

```typescript
const [formData, setFormData] = useState({ email: "", password: "" });
const [errors, setErrors] = useState({});

const validateForm = () => {
  const newErrors = {};
  if (!formData.email.includes("@")) newErrors.email = "Invalid email address";
  if (formData.password.length < 6)
    newErrors.password = "Password must be at least 6 characters long";

  return newErrors;
  // errors 상태를 주입하지 않고, 순수함수로 만듬
};

const handleSubmit = () => {
  const validateErrors = validateForm();
  if (Object.keys(validationErrors).length > 0) {
    setErorrs(validationErrors);
  } else {
    console.log("Form submitted successfully:", formData);
  }
};

return (
    <div>
        <input type="email" name="email" value={formData.email}
        onChange={(e)=> setFormData({...formData, email: e.target.value})}
        {errors.email && <span>{errors.email}</span>}
        <input type="password" name="password" value={formData.password} onChange={(e)=> setFormData({...formData, password: e.target.value})}/>
        {errors.password && <span>{erorrs.password}</span>}
        <button onClick={handleSubmit}>Submit</button>
    </div>
)
```
